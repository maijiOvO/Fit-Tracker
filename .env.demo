# 仅 `npm run build:demo`（vite build --mode demo）加载。
#
# 演示构建：公开可访问、完全不联网、每次打开清空。
# 用途是让人在浏览器里直接试这个 app，而不是展示真实数据。
#
# ⚠️ 这里**绝对不能**出现 VITE_API_KEY / VITE_API_KEY_DEV。
#    vite 会把 VITE_ 开头的变量明文烙进 JS 产物，
#    公开部署等于把凭据贴在网上。演示构建也压根不需要它们 ——
#    services/appEnv.ts 的 isDemo() 会让 isRemoteConfigured() 恒为 false。
VITE_FITLOG_DEMO=true
