# 仅 `npm run build:release`（vite build --mode release）加载。
#
# 这是全项目**唯一**把产物烙印成生产数据环境的地方。
# 普通 `npm run build` / `vite preview` 不加载本文件 → 产物默认 dev 环境，
# 于是本机预览生产包时也碰不到真实用户数据。
#
# ⚠️ 打 APK 必须用 `npm run build:release`，不能用 `npm run build`。
#    万一用错了，services/appEnv.ts 里的 Capacitor 原生检测会兜底强制 prod。
VITE_FITLOG_ENV=prod
