# 架构与重构规范
- 目标：将应用改造成完全单用户、本地化、由个人服务器存储的健身记录工具。
- 禁止：禁止引入任何与注册、登录、JWT 刷新、验证码相关的第三方库和复杂逻辑。
- 代码风格：拒绝过度嵌套（超过 3 层嵌套直接重构），优先使用早期返回（Early Return）模式。
- 容错要求：所有网络请求（个人服务器 API）必须包裹防御性捕获，在失败时返回清晰的、人类和 Agent 都能读懂的中文错误日志。

# [Harness: 全局验证循环 (Validation Loop)]
- 全局行为约束：在你（Agent）完成任何新功能的添加、既有代码的修改、或者重构之后，必须主动在终端执行 `npm run validate`。
- 容错处理：
  1. 如果验证脚本报错，你必须读取终端的错误日志。
  2. 针对报错进行自我纠错（Fix），然后再次运行 `npm run validate`。
  3. 在 `npm run validate` 连续通过之前，绝对不允许要求用户进行手动测试，也不允许结束当前任务。
  # [环境与密钥管理]
- 环境变量规范：本项目所有的外部服务配置（如个人服务器的 URL 和 API 密钥）都必须通过环境变量读取。
- 认知引导：
  1. 本项目使用 Vite 框架，所有暴露给前端的环境变量必须以 `VITE_` 开头。
  2. 当你需要执行需要鉴权的终端命令时，请主动从本地的 `.env.local` 文件中读取真实的值，但**绝对禁止将读取到的真实密钥打印在聊天窗口或代码注释中**。
  3. 在代码层面，统一使用 `import.meta.env.VITE_XXX` 的方式调用环境变量，**绝对禁止使用 `process.env`**，也禁止硬编码。

# [浏览器调试与运行时校验]
- 全局行为约束：在你进行任何"模拟用户行为 / 端到端测试 / UI 回归验证"时，必须主动要求用户提供浏览器开发者工具 Console 与 Network 面板中的红色 / 黄色报错，并据此定位问题。
- 拒绝盲测：在没有读取过开发者模式报错的情况下，禁止给出"功能正常"或"测试通过"的结论。
- 噪音过滤：来自浏览器扩展（`chrome-extension://...`）、Chrome React DevTools 提示、以及 `apple-mobile-web-app-capable` 等 deprecation 警告应明确标记为"非项目报错"，不要追修。
- 报错最小集：每次定位问题时，至少要求用户提供：
  1. 第一条 ❌ Error 的完整堆栈；
  2. 最早出现的 ❌ Network 失败请求（URL / 状态码 / 响应预览）；
  3. Service Worker 注册或 fetch 阶段的报错（若有）。